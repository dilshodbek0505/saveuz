# Решение ошибки 413 Request Entity Too Large

## ✅ Что уже сделано автоматически:

1. **Настройки Django** (`core/settings.py`):
   - Увеличены лимиты загрузки до 10MB
   - Увеличен лимит полей формы

2. **Автоматический скрипт** (`scripts/setup_nginx_upload_limit.sh`):
   - Автоматически настраивает nginx при деплое
   - Создает резервные копии
   - Проверяет синтаксис

3. **GitHub Actions** (`.github/workflows/deploy.yml`):
   - Автоматически запускает настройку nginx при каждом деплое

## 🚀 Что нужно сделать СЕЙЧАС:

### Вариант 1: Автоматический (рекомендуется)

Просто запустите на сервере один раз:

```bash
cd /var/www/saveuz
sudo bash scripts/setup_nginx_upload_limit.sh
```

### Вариант 2: Через GitHub Actions

При следующем push в `main` скрипт запустится автоматически. Но для немедленного решения используйте Вариант 1.

### Вариант 3: Ручная настройка

Если автоматические способы не работают, см. `QUICK_FIX.md`

## 📋 Что делает скрипт:

1. Находит конфиг nginx для admin.saveuz.uz
2. Создает резервную копию
3. Добавляет/обновляет настройки:
   - `client_max_body_size 20M`
   - `client_body_timeout 60s`
   - `client_header_timeout 60s`
   - `client_body_buffer_size 128k`
   - Таймауты прокси (если нужно)
4. Проверяет синтаксис nginx
5. Перезагружает nginx

## ✨ Результат:

После выполнения скрипта можно будет загружать:
- Изображения до 5MB каждое (лимит в Django)
- Несколько изображений общим размером до 20MB
- Ошибка 413 больше не будет появляться

## 🔍 Проверка:

После настройки попробуйте добавить продукт в общей базе с изображением. Ошибка должна исчезнуть.
